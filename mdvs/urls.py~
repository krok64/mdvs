from django.conf.urls import patterns, include, url

# Uncomment the next two lines to enable the admin:
# from django.contrib import admin
# admin.autodiscover()

from gost24950 import views
from django.conf import settings

urlpatterns = patterns('gost24950.views',
    url(r'^$', views.main),
    url(r'^gost24950/$', 'show_gost24950'),
    url(r'^vgidro/$', 'show_VGidro'),
    url(r'^massatr/$', 'show_MassaTr'),
    url(r'^vimpuls/$', 'show_VImpuls'),
    url(r'^vsgaza/$', 'show_VSGaza'),
    url(r'^transh/$', 'show_Transh'),
    url(r'^srezka/$', 'show_Srezka'),
    url(r'^kotlovan/$', 'show_Kotlovan'),
    url(r'^vkotlovan/$', 'show_VKotlovan'),
    url(r'^valik/$', 'show_Valik'),
    url(r'^sovug/$', 'show_SovUg'),
    url(r'^progal/$', 'show_Progal'),
    url(r'^gaztu/$', 'show_GazTU'),
    url(r'^pipe/$', 'show_Pipe'),
    url(r'^pipe/delete/(?P<id>\d+)/$', 'show_Pipe'),
    url(r'^pipe/select/(?P<pipe_id>\d+)/$', 'show_PipeObj', name='show_PipeObj'),
    url(r'^pipe/obj/(?P<pipe_id>\d+)/(?P<action>del)/(?P<obj_id>\d+)/$', 'show_PipeObj'),
    url(r'^pipe/obj/(?P<pipe_id>\d+)/(?P<action>ed)/(?P<obj_id>\d+)/$', 'show_PipeObjEd'),
    url(r'^proj/', include('gen.urls')),
    # Examples:
    # url(r'^$', 'mdvs.views.home', name='home'),
    # url(r'^mdvs/', include('mdvs.foo.urls')),

    # Uncomment the admin/doc line below to enable admin documentation:
    # url(r'^admin/doc/', include('django.contrib.admindocs.urls')),

    # Uncomment the next line to enable the admin:
    # url(r'^admin/', include(admin.site.urls)),
)
